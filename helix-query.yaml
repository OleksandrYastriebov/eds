indices:
  site:
    include:
      - /en/**
    target: /query-index.json
    properties:
      page-header-1:
        select: h1
        value: |
          el.textContent.trim()
      page-header-2:
        select: h2
        value: |
          el.textContent.trim()